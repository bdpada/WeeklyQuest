export async function registerUserPlaceholder() {
  // TODO: Hash passwords with bcrypt and persist users with Prisma.
  return { message: 'Register service placeholder' };
}

export async function loginUserPlaceholder() {
  // TODO: Verify credentials and issue JWT cookies.
  return { message: 'Login service placeholder' };
}
